using UnityEngine;

namespace Game.GUI.Controller
{
    public class SideBarButtonController : MonoBehaviour
    {
    
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }

  
    }
}
