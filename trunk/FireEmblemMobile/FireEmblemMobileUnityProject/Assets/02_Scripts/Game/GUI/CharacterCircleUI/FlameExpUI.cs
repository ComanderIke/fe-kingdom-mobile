using UnityEngine;

namespace Game.GUI.CharacterCircleUI
{
    public class FlameExpUI : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
