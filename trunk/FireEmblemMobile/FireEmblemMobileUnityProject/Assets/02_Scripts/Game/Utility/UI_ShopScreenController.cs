namespace Game.Utility
{
    public class UI_ShopScreenController : TweenableUIController
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
